//epic: given arr, what's the minimum number of swaps you can make to put the numbers in order.
//arr is an array of consecutive digits 1-start

//check if elem is where it should be
//if not, put it where it should be

function minimumSwaps(arr) {}

minimumSwaps([2, 3, 1, 4]);
minimumSwaps([7, 1, 3, 2, 4, 5, 6]);
